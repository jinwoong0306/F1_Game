# F1 레이싱 게임 협업 가이드

## 목차
1. [프로젝트 개요](#프로젝트-개요)
2. [개발 환경 설정](#개발-환경-설정)
3. [Git 브랜치 전략](#git-브랜치-전략)
4. [작업 시작하기](#작업-시작하기)
5. [코드 작성 가이드](#코드-작성-가이드)
6. [커밋 및 푸시](#커밋-및-푸시)
7. [Pull Request 생성](#pull-request-생성)
8. [충돌 해결](#충돌-해결)
9. [팀별 작업 영역](#팀별-작업-영역)

---

## 프로젝트 개요

**F1 레이싱 게임**은 libGDX 프레임워크를 사용한 2D 레이싱 게임입니다.

### 기술 스택
- **프레임워크**: libGDX 1.13.1
- **언어**: Java 17
- **빌드 도구**: Gradle 8.10.2
- **물리 엔진**: Box2D
- **맵 에디터**: Tiled
- **네트워킹**: KryoNet 2.22.0-RC1

### 주요 기능
- ✅ 싱글플레이 모드
- ✅ Tiled 맵 기반 트랙 시스템
- ✅ 체크포인트 및 랩 카운팅
- ✅ HUD (속도계, 랩타임, 내구도, 타이어)
- ✅ 피트스톱 미니게임
- ✅ Grass 영역 속도 감소
- 🚧 멀티플레이 모드 (개발 중)

---

## 개발 환경 설정

### 1. 필수 요구사항
- **Java JDK 17** 이상
- **Git** 2.40 이상
- **IDE**: IntelliJ IDEA 또는 VSCode

### 2. 프로젝트 클론

```bash
git clone https://github.com/H0GUN3/F1_Game.git
cd F1_Game
```

### 3. 프로젝트 빌드 및 실행

```bash
# 빌드
./gradlew build

# 게임 실행 (LWJGL3 데스크톱)
./gradlew lwjgl3:run

# 서버 실행
./gradlew server:run
```

### 4. IDE 설정

#### IntelliJ IDEA
1. `File > Open` → F1 프로젝트 폴더 선택
2. Gradle 자동 import 대기
3. `Run/Debug Configurations` → `+` → `Gradle`
   - Name: `Run Game`
   - Gradle project: `F1:lwjgl3`
   - Tasks: `run`

#### VSCode
1. Java Extension Pack 설치
2. Gradle for Java 설치
3. 프로젝트 폴더 열기

---

## Git 브랜치 전략

### 브랜치 구조

```
main (프로덕션, 항상 안정적인 상태 유지)
  └── feature/singleplay-polish (싱글플레이 개선)
  └── feature/multiplayer-core (멀티플레이 개발)
```

### 브랜치 규칙

- **main**: 배포 가능한 안정적인 코드만 병합
- **feature/**: 기능 개발 브랜치
  - `feature/multiplayer-core`: 멀티플레이어 네트워킹
  - `feature/singleplay-polish`: 싱글플레이 UI/로직 개선

### 브랜치 명명 규칙
- `feature/기능명`: 새 기능 개발
- `fix/버그명`: 버그 수정
- `refactor/대상`: 리팩토링
- 예시: `feature/multiplayer-room`, `fix/checkpoint-rotation`

---

## 작업 시작하기

### 1. 최신 코드 받기

```bash
# main 브랜치 최신화
git checkout main
git pull origin main
```

### 2. 작업 브랜치 생성 (또는 기존 브랜치로 전환)

#### 싱글플레이 개선 팀
```bash
git checkout -b feature/singleplay-polish
git push -u origin feature/singleplay-polish
```

#### 멀티플레이 개발 팀
```bash
git checkout feature/multiplayer-core
git pull origin feature/multiplayer-core
```

### 3. 매일 작업 시작 전 동기화

```bash
# 원격 최신 변경사항 확인
git fetch origin

# main 브랜치의 변경사항 병합 (충돌 방지)
git checkout main
git pull origin main

# 작업 브랜치로 돌아가서 main 내용 병합
git checkout feature/your-branch-name
git merge main

# 충돌 발생 시 해결 후:
git add .
git commit -m "Merge main into feature branch"
```

---

## 코드 작성 가이드

### 프로젝트 구조

```
F1/
├── assets/               # 게임 에셋
│   ├── atlas/           # TextureAtlas (game_ui.atlas)
│   ├── cars/            # 차량 이미지
│   ├── ui/              # UI 이미지
│   ├── fonts/           # 폰트 파일
│   └── *.tmx            # Tiled 맵 파일
├── core/src/main/java/com/mygame/f1/
│   ├── Main.java        # 게임 진입점
│   ├── GameScreen.java  # 게임플레이 화면
│   ├── screens/         # 화면 클래스
│   │   ├── LoginScreen.java
│   │   ├── MainMenuScreen.java
│   │   ├── SinglePlayScreen.java
│   │   └── MultiplayerPlaceholderScreen.java
│   ├── ui/              # UI 컴포넌트
│   │   └── SkinFactory.java
│   └── network/         # 네트워크 (개발 중)
├── server/src/main/java/com/mygame/f1/server/
│   ├── GameServer.java  # 서버 메인
│   └── PacketRegistry.java
└── shared/src/          # 공용 코드 (클라이언트/서버)
```

### 코딩 컨벤션

#### Java 코드 스타일
```java
// 클래스명: PascalCase
public class GameScreen implements Screen { }

// 메서드명: camelCase
private void updateCarPhysics(float delta) { }

// 상수: UPPER_SNAKE_CASE
private static final int MAX_PLAYERS = 4;

// 변수명: camelCase
private int currentLap = 0;
```

#### 주석 스타일
```java
// 한 줄 주석: 짧은 설명
int speed = 100; // km/h 단위

/**
 * 여러 줄 주석: 메서드 설명
 * @param delta 프레임 시간 (초 단위)
 */
public void update(float delta) { }
```

### 중요한 코드 패턴

#### 1. Skin 사용 (공유 인스턴스)
```java
// ✅ 올바른 방법
Skin skin = Main.getSharedSkin();
// ❌ 잘못된 방법
skin.dispose(); // Main이 관리하므로 dispose 금지!
```

#### 2. TextureAtlas 사용
```java
// 초기화 시 한 번만 호출
TextureAtlas atlas = Main.assetManager.get("atlas/game_ui.atlas", TextureAtlas.class);
TextureRegion region = atlas.findRegion("icon-car");

// ❌ render() 메서드에서 매 프레임 findRegion() 호출 금지!
```

#### 3. Box2D 좌표 변환
```java
// PPM (Pixels Per Meter) 상수 사용
private static final float PPM = 32f;

// 화면 좌표 → Box2D 좌표
float worldX = screenX / PPM;

// Box2D 좌표 → 화면 좌표
float screenX = worldX * PPM;
```

---

## 커밋 및 푸시

### 커밋 메시지 규칙

```
<타입>: <제목>

<본문 (선택사항)>
```

#### 타입 종류
- `feat`: 새로운 기능 추가
- `fix`: 버그 수정
- `refactor`: 코드 리팩토링
- `style`: 코드 포맷팅
- `docs`: 문서 수정
- `test`: 테스트 코드 추가
- `chore`: 빌드 업무, 패키지 매니저 설정 등

#### 예시
```bash
# 좋은 커밋 메시지
git commit -m "feat: 멀티플레이어 룸 생성 UI 추가"
git commit -m "fix: 회전된 체크포인트 객체 인식 버그 수정"
git commit -m "refactor: 네트워크 패킷 처리를 별도 클래스로 분리"

# 나쁜 커밋 메시지
git commit -m "수정"
git commit -m "버그 고침"
git commit -m "ㅁㄴㅇㄹ"
```

### 커밋 및 푸시 워크플로우

```bash
# 1. 변경 파일 확인
git status

# 2. 변경 파일 스테이징
git add core/src/main/java/com/mygame/f1/GameScreen.java
# 또는 전체 추가 (신중하게!)
git add .

# 3. 커밋
git commit -m "feat: 플레이어 위치 동기화 구현"

# 4. 원격 저장소에 푸시
git push origin feature/multiplayer-core
```

### 자주 커밋하기
```bash
# 의미 있는 단위로 자주 커밋 (하루 3~5회 권장)
git add network/GameClient.java
git commit -m "feat: TCP 연결 핸들러 추가"

git add network/packets/PositionPacket.java
git commit -m "feat: 위치 업데이트 패킷 구조 정의"

git push origin feature/multiplayer-core
```

---

## Pull Request 생성

### 1. GitHub에서 PR 생성

1. https://github.com/H0GUN3/F1_Game 접속
2. `Pull requests` 탭 클릭
3. `New pull request` 클릭
4. Base: `main` ← Compare: `feature/your-branch-name`
5. PR 제목 작성:
   ```
   feat: 멀티플레이어 룸 시스템 구현
   ```
6. PR 설명 작성:
   ```markdown
   ## 변경 사항
   - 멀티플레이어 룸 생성/입장 UI 구현
   - 서버 RoomManager 클래스 추가
   - 패킷: CreateRoomPacket, JoinRoomPacket

   ## 테스트
   - [x] 룸 생성 기능 동작 확인
   - [x] 최대 4명 입장 제한 확인
   - [ ] 실제 게임 시작 (다음 PR에서)

   ## 스크린샷
   (UI 스크린샷 첨부)
   ```

### 2. 코드 리뷰 요청

- Reviewers에서 팀원 지정
- Labels 추가: `feature`, `multiplayer`, `work-in-progress` 등

### 3. 리뷰 피드백 반영

```bash
# 피드백에 따른 수정 후
git add .
git commit -m "refactor: 코드 리뷰 피드백 반영 - 메서드 추출"
git push origin feature/multiplayer-core

# PR이 자동으로 업데이트됨
```

### 4. 병합

- 리뷰 승인 후 `Merge pull request` 클릭
- 병합 후 브랜치 삭제 옵션 선택

---

## 충돌 해결

### 충돌 발생 시나리오

```bash
# main 브랜치의 최신 변경사항을 가져올 때
git checkout feature/multiplayer-core
git merge main

# 충돌 발생!
Auto-merging core/src/main/java/com/mygame/f1/GameScreen.java
CONFLICT (content): Merge conflict in core/src/main/java/com/mygame/f1/GameScreen.java
Automatic merge failed; fix conflicts and then commit the result.
```

### 충돌 해결 방법

#### 1. 충돌 파일 확인
```bash
git status
# Unmerged paths:
#   both modified:   core/src/main/java/com/mygame/f1/GameScreen.java
```

#### 2. 파일 열어서 충돌 확인

```java
<<<<<<< HEAD (현재 브랜치)
private void updateCarPhysics(float delta) {
    // 멀티플레이어 동기화
    if (isMultiplayer) {
        sendPositionUpdate();
    }
}
=======
private void updateCarPhysics(float delta) {
    // 새로운 브레이크 로직
    if (isBraking) {
        velocity *= 0.85f;
    }
}
>>>>>>> main (병합할 브랜치)
```

#### 3. 충돌 해결 (두 변경사항 모두 포함)

```java
private void updateCarPhysics(float delta) {
    // 새로운 브레이크 로직 (main)
    if (isBraking) {
        velocity *= 0.85f;
    }

    // 멀티플레이어 동기화 (feature/multiplayer-core)
    if (isMultiplayer) {
        sendPositionUpdate();
    }
}
```

#### 4. 충돌 해결 완료

```bash
git add core/src/main/java/com/mygame/f1/GameScreen.java
git commit -m "Merge main into multiplayer-core - 충돌 해결"
git push origin feature/multiplayer-core
```

### 충돌 최소화 팁

1. **자주 동기화**: 매일 main 브랜치 내용을 병합
2. **작은 단위 커밋**: 큰 변경사항을 한 번에 커밋하지 않기
3. **작업 영역 분리**: 팀별로 다른 파일 작업 (아래 참고)

---

## 팀별 작업 영역

### 싱글플레이 개선 팀 (`feature/singleplay-polish`)

#### 주요 작업 파일
```
core/src/main/java/com/mygame/f1/
├── GameScreen.java
│   ├── updateCarPhysics()      # 차량 물리 (속도, 브레이크)
│   ├── handleInput()           # 입력 처리
│   ├── drawSpeedHud()          # 속도계 HUD
│   ├── drawDurabilityHud()     # 내구도 HUD
│   └── drawTireHud()           # 타이어 HUD
└── screens/
    └── SinglePlayScreen.java   # 차량 선택 UI
```

#### 작업 목표
- [ ] 차량 물리 개선 (가속, 브레이크 감각)
- [ ] HUD 크기/위치 조정
- [ ] 싱글플레이 차량 선택 화면 개선
- [ ] 피트스톱 미니게임 밸런싱

#### 작업 시작
```bash
git checkout -b feature/singleplay-polish
git push -u origin feature/singleplay-polish

# 작업 후
git add core/src/main/java/com/mygame/f1/GameScreen.java
git commit -m "feat: 차량 브레이크 물리 개선"
git push origin feature/singleplay-polish
```

---

### 멀티플레이 개발 팀 (`feature/multiplayer-core`)

#### 주요 작업 파일
```
core/src/main/java/com/mygame/f1/
├── network/                    # 새로 생성
│   ├── GameClient.java         # 클라이언트 연결 관리
│   ├── NetworkManager.java     # 네트워크 이벤트 처리
│   └── packets/                # 패킷 정의
│       ├── PositionUpdatePacket.java
│       ├── PlayerJoinPacket.java
│       └── LapCompletePacket.java
├── GameScreen.java
│   ├── sendPositionUpdate()    # 위치 동기화 (멀티플레이어만)
│   ├── handleServerPackets()   # 서버 패킷 처리
│   └── renderOtherPlayers()    # 다른 플레이어 렌더링
└── screens/
    └── MultiplayerPlaceholderScreen.java  # 룸 목록 UI

server/src/main/java/com/mygame/f1/server/
├── GameServer.java             # 서버 메인 로직
├── RoomManager.java            # 방 생성/관리 (새로 생성)
├── GameRoom.java               # 게임 방 (새로 생성)
└── PacketRegistry.java         # 패킷 등록

shared/src/                     # 새로 생성
└── com/mygame/f1/shared/
    └── packets/                # 클라이언트/서버 공용 패킷
```

#### 작업 목표
- [ ] 클라이언트-서버 연결 구조
- [ ] 방 생성/입장 시스템
- [ ] 플레이어 스폰 동기화
- [ ] 위치/속도 실시간 동기화
- [ ] 체크포인트/랩 동기화
- [ ] 레이스 시작 카운트다운 동기화

#### 작업 시작
```bash
git checkout feature/multiplayer-core
git pull origin feature/multiplayer-core

# 작업 후
git add core/src/main/java/com/mygame/f1/network/
git commit -m "feat: 클라이언트-서버 연결 핸들러 구현"
git push origin feature/multiplayer-core
```

---

## 개발 단계별 로드맵

### Phase 1: 기반 구축 (1-2주) ✅ 완료
- [x] TextureAtlas 마이그레이션
- [x] HUD 시스템 복원
- [x] Tiled 맵 상호작용 (체크포인트, Grass)

### Phase 2: 싱글플레이 완성 (1-2주) 🚧 진행 중
- [ ] 차량 물리 개선
- [ ] HUD 레이아웃 최적화
- [ ] UI/UX 개선

### Phase 3: 멀티플레이 기반 (2-3주) 🚧 진행 중
- [x] 브랜치 구조 설정
- [ ] 네트워크 연결 구조
- [ ] 방 시스템 구현
- [ ] 플레이어 스폰 동기화

### Phase 4: 멀티플레이 완성 (2-3주)
- [ ] 위치 동기화
- [ ] 체크포인트/랩 동기화
- [ ] 레이스 시작 동기화

---

## 문제 해결

### 빌드 실패
```bash
# Gradle 캐시 초기화
./gradlew clean build --refresh-dependencies
```

### 게임 실행 시 "Asset not found" 에러
```bash
# assets 폴더가 올바른지 확인
ls -la assets/atlas/game_ui.atlas
ls -la assets/cars/
```

### Git 푸시 실패 (인증)
```bash
# GitHub Personal Access Token 사용
# Settings > Developer settings > Personal access tokens > Generate new token
# repo 권한 체크 후 생성

# 푸시 시 토큰을 비밀번호로 사용
git push origin feature/multiplayer-core
Username: your-username
Password: ghp_xxxxxxxxxxxxxxxxxxxx (토큰 붙여넣기)
```

---

## 연락처 및 리소스

- **GitHub**: https://github.com/H0GUN3/F1_Game
- **libGDX 문서**: https://libgdx.com/wiki/
- **Tiled 문서**: https://doc.mapeditor.org/
- **KryoNet 문서**: https://github.com/EsotericSoftware/kryonet

---

**마지막 업데이트**: 2025-12-03
